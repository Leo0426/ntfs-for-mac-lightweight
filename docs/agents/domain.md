# 领域文档

本仓库采用单一上下文。

## 探索顺序

工程类 skills 在操作代码前：

1. 读取仓库根目录的 `CONTEXT.md`。
2. 读取 `docs/adr/` 中与当前代码区域有关的 ADR。
3. 如果文件尚不存在，静默继续；只在术语或架构决策确有需要时创建。

## 领域语言

Issue 标题、测试名称、重构提案和代码命名应使用 `CONTEXT.md` 定义的术语。

如果概念不在词汇表中，应先判断是命名漂移还是领域模型确有缺口。

## ADR 冲突

如果实现或建议与现有 ADR 冲突，必须明确指出对应 ADR 和冲突原因，不得静默覆盖。
